import { requireProfile } from '@/lib/auth'; import { createClient } from '@/lib/supabase/server'; import { createContentAction } from '@/lib/actions/content'; import { PageHeader,Badge,Empty } from '@/components/UI'; import { AdminForm,ClientSelect } from '@/components/AdminForms'; import { dateLabel } from '@/lib/utils';
export default async function AdminContent(){await requireProfile(true);const s=await createClient();const [{data:clients},{data:items}]=await Promise.all([s.from('clients').select('id,name').order('name'),s.from('content_items').select('*,clients(name)').order('created_at',{ascending:false})]);return <><PageHeader eyebrow="ADMIN" title="Manage content" description="Add posts to a client calendar and track approval status."/><section className="admin-grid"><AdminForm title="Add content" action={createContentAction}><ClientSelect clients={clients||[]}/><label>Post title<input name="title" required/></label><div className="field-row"><label>Content type<select name="content_type"><option>Reel</option><option>Carousel</option><option>Photo</option><option>Story Set</option><option>Graphic</option></select></label><label>Status<select name="status"><option value="draft">Draft</option><option value="internal_review">Internal review</option><option value="client_review">Client review</option><option value="approved">Approved</option><option value="scheduled">Scheduled</option><option value="posted">Posted</option></select></label></div><label>Scheduled for<input name="scheduled_for" type="datetime-local"/></label><label>Preview image URL<input name="preview_url" type="url" placeholder="https://..."/></label><label>Caption<textarea name="caption" rows={6}/></label></AdminForm><div className="card"><h2>Recent content</h2>{items?.length?items.map((i:any)=><div className="list-row" key={i.id}><div><strong>{i.title}</strong><small>{i.clients?.name} · {dateLabel(i.scheduled_for)}</small></div><Badge value={i.status}/></div>):<Empty title="No content" body="Add the first post to a client calendar."/>}</div></section></>}
