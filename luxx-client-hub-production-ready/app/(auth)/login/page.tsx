import { loginAction, forgotPasswordAction } from '@/lib/actions/auth';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const p=await searchParams;return <main className="login-page"><section className="login-brand"><div className="brand-mark">L</div><p>LUXX SOCIALS</p><h1>Your marketing,<br/>beautifully organized.</h1><span>Review content, approve posts and access everything your brand needs.</span></section><section className="login-panel"><div className="login-card"><p className="eyebrow">CLIENT PORTAL</p><h2>Welcome back</h2><p className="muted">Sign in to your Luxx Client Hub.</p>{p.error&&<div className="alert error">{p.error}</div>}{p.message&&<div className="alert success">{p.message}</div>}<form action={loginAction} className="stack"><label>Email<input name="email" type="email" required placeholder="you@company.com"/></label><label>Password<input name="password" type="password" required placeholder="••••••••"/></label><button className="button primary">Sign in</button></form><details><summary>Forgot password?</summary><form action={forgotPasswordAction} className="stack compact"><input name="email" type="email" required placeholder="Email address"/><button className="button secondary">Send reset email</button></form></details></div></section></main>}
