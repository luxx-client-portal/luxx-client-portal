import { requireProfile } from '@/lib/auth'; import { createClient } from '@/lib/supabase/server'; import { createDocumentAction } from '@/lib/actions/portal'; import { PageHeader,Empty } from '@/components/UI'; import { AdminForm,ClientSelect } from '@/components/AdminForms';
export default async function AdminDocuments(){await requireProfile(true);const s=await createClient();const [{data:clients},{data:docs}]=await Promise.all([s.from('clients').select('id,name').order('name'),s.from('documents').select('*,clients(name)').order('created_at',{ascending:false})]);return <><PageHeader eyebrow="ADMIN" title="Manage documents" description="Share contracts, reports and downloadable files with clients."/><section className="admin-grid"><AdminForm title="Add document" action={createDocumentAction}><ClientSelect clients={clients||[]}/><label>Document name<input name="name" required/></label><label>Category<select name="category"><option>Contract</option><option>Invoice</option><option>Monthly Report</option><option>Brand Asset</option><option>Other</option></select></label><label>Public or signed file URL<input name="file_path" type="url" required placeholder="https://..."/></label><p className="form-note">For private files, use a signed Supabase Storage URL.</p></AdminForm><div className="card"><h2>Shared documents</h2>{docs?.length?docs.map((d:any)=><div className="list-row" key={d.id}><div><strong>{d.name}</strong><small>{d.clients?.name} · {d.category}</small></div></div>):<Empty title="No documents" body="Add the first document."/>}</div></section></>}
