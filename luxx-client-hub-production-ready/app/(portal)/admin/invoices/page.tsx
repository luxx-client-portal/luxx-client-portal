import { requireProfile } from '@/lib/auth'; import { createClient } from '@/lib/supabase/server'; import { createInvoiceAction } from '@/lib/actions/portal'; import { PageHeader,Badge,Empty } from '@/components/UI'; import { AdminForm,ClientSelect } from '@/components/AdminForms'; import { money } from '@/lib/utils';
export default async function AdminInvoices(){await requireProfile(true);const s=await createClient();const [{data:clients},{data:invoices}]=await Promise.all([s.from('clients').select('id,name').order('name'),s.from('invoices').select('*,clients(name)').order('due_date',{ascending:false})]);return <><PageHeader eyebrow="ADMIN" title="Manage invoices" description="Add invoice records and payment status for each client."/><section className="admin-grid"><AdminForm title="Add invoice" action={createInvoiceAction}><ClientSelect clients={clients||[]}/><label>Invoice number<input name="invoice_number" required placeholder="BMP-0726"/></label><div className="field-row"><label>Amount<input name="amount" type="number" step="0.01" min="0" required/></label><label>Status<select name="status"><option value="due">Due</option><option value="paid">Paid</option><option value="overdue">Overdue</option></select></label></div><label>Due date<input name="due_date" type="date"/></label><label>Invoice file URL<input name="file_path" type="url" placeholder="https://..."/></label></AdminForm><div className="card"><h2>Invoice records</h2>{invoices?.length?invoices.map((i:any)=><div className="list-row" key={i.id}><div><strong>{i.invoice_number}</strong><small>{i.clients?.name} · {money(i.amount_cents)}</small></div><Badge value={i.status}/></div>):<Empty title="No invoices" body="Add the first invoice record."/>}</div></section></>}
